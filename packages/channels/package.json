{
  "name": "@lets-talk/channels",
  "version": "1.0.0",
  "description": "Handles channels for communication between apps either on iframes, popups or full windows",
  "scripts": {
    "prepublishOnly": "tsc",
    "test": "jest --no-cache --coverage",
    "test:ci": "jest --no-cache --coverage --runInBand"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "author": "Sandino Nunez",
  "license": "ISC",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/jest": "^24.0.0",
    "jest": "^23.5.0",
    "ts-jest": "^24.0.0"
  },
  "dependencies": {
    "@types/json-stringify-safe": "^5.0.0",
    "@types/node": "^12.6.6",
    "debug": "^4.1.1",
    "global": "^4.4.0",
    "json-stringify-safe": "^5.0.1",
    "post-robot": "^10.0.18"
  }
}
